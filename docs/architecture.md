# FolioForge — System Architecture

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              CLIENTS                                    │
│                                                                         │
│   ┌─────────────┐    ┌─────────────┐    ┌──────────────────┐           │
│   │  🌐 Browser  │    │ 📱 Mobile   │    │ 🔗 Public Link   │           │
│   │  (Dashboard) │    │  (Browser)  │    │ /portfolio/[usr] │           │
│   └──────┬──────┘    └──────┬──────┘    └────────┬─────────┘           │
│          │                  │                     │                      │
└──────────┼──────────────────┼─────────────────────┼─────────────────────┘
           │                  │                     │
           ▼                  ▼                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        NEXT.JS APP ROUTER (Vercel)                      │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                      Server Components                             │ │
│  │                                                                    │ │
│  │  ┌──────────┐ ┌───────────┐ ┌────────────┐ ┌──────────────────┐  │ │
│  │  │ Landing  │ │ Dashboard │ │ AI Builder │ │ Portfolio (SSR)  │  │ │
│  │  │ page.tsx │ │ page.tsx  │ │ page.tsx   │ │ [username]/[slug]│  │ │
│  │  └──────────┘ └───────────┘ └────────────┘ └──────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                       Client Components                            │ │
│  │                                                                    │ │
│  │  ┌──────────────┐ ┌───────────────┐ ┌────────────────────────┐   │ │
│  │  │ Intro Anim   │ │ Figma Gallery │ │ Language Provider (i18n)│   │ │
│  │  │ Particles    │ │ Onboarding    │ │ Dark Mode Toggle       │   │ │
│  │  └──────────────┘ └───────────────┘ └────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                         API Routes                                 │ │
│  │                                                                    │ │
│  │  /api/auth/*          ← NextAuth (JWT, Credentials)               │ │
│  │  /api/register        ← สมัครสมาชิก + ส่ง email verify            │ │
│  │  /api/verify-email    ← ยืนยัน email                              │ │
│  │  /api/projects        ← CRUD โปรเจกต์                             │ │
│  │  /api/ai-generate     ← สร้างพอร์ตด้วย AI                        │ │
│  │  /api/portfolio-pages ← จัดการหน้าพอร์ตโฟลิโอ                     │ │
│  │  /api/upload          ← อัพโหลดไฟล์                               │ │
│  │  /api/figma           ← ดึงดีไซน์จาก Figma                       │ │
│  │  /api/profile         ← แก้ไขโปรไฟล์                              │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │                     Middleware / Auth                               │ │
│  │                                                                    │ │
│  │  NextAuth v5 (JWT) ──── session: { id, email, username, name }    │ │
│  │  Admin gate: email === "piyawat211149@gmail.com"                   │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                         │
└────────┬──────────┬──────────────┬──────────────┬───────────────────────┘
         │          │              │              │
         ▼          ▼              ▼              ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                         EXTERNAL SERVICES                                │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │ 🐘 Neon      │  │ 🤖 Google    │  │ 📦 Vercel   │  │ 🎨 Figma   │  │
│  │  PostgreSQL  │  │  Gemini AI   │  │  Blob Store │  │  API v1    │  │
│  │              │  │              │  │             │  │            │  │
│  │ Users        │  │ สร้างเนื้อหา  │  │ รูปภาพ      │  │ ดึงดีไซน์   │  │
│  │ Projects     │  │ พอร์ตโฟลิโอ  │  │ ไฟล์แนบ     │  │ Thumbnails │  │
│  │ Pages        │  │ หลายหน้า     │  │ Avatars     │  │ Pages      │  │
│  │ Files        │  │ อัตโนมัติ     │  │             │  │            │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬──────┘  └─────┬──────┘  │
│         │                 │                  │               │          │
│         ▼                 ▼                  ▼               ▼          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │                    Connection Details                             │   │
│  │                                                                  │   │
│  │  Neon DB    : Prisma ORM (DATABASE_URL + DIRECT_URL)            │   │
│  │  Gemini AI  : REST API  (GEMINI_API_KEY)                        │   │
│  │  Vercel Blob: SDK       (BLOB_READ_WRITE_TOKEN)                 │   │
│  │  Figma      : REST API  (FIGMA_API_TOKEN + FIGMA_TEAM_ID)      │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────┐  ┌──────────────┐                                     │
│  │ 📧 Gmail     │  │ 🎨 Canva     │                                     │
│  │  SMTP        │  │  Templates   │                                     │
│  │              │  │              │                                     │
│  │ ส่ง email    │  │ ลิงก์ template│                                     │
│  │ verify       │  │ เปิดแก้ไขใน  │                                     │
│  │              │  │ Canva ตรง    │                                     │
│  └──────────────┘  └──────────────┘                                     │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘


┌──────────────────────────────────────────────────────────────────────────┐
│                          DATA FLOW                                       │
│                                                                          │
│  [User] ──สมัคร──▶ [API /register] ──บันทึก──▶ [Neon DB]               │
│                           │                                              │
│                     ส่ง email verify ──▶ [Gmail SMTP]                    │
│                                                                          │
│  [User] ──login──▶ [NextAuth] ──JWT──▶ [Session]                        │
│                                                                          │
│  [User] ──สร้างพอร์ต──▶ [API /ai-generate] ──prompt──▶ [Gemini AI]     │
│                                │                                         │
│                          ◀── AI response (multi-page JSON)               │
│                                │                                         │
│                          บันทึก──▶ [Neon DB: PortfolioPage]             │
│                                                                          │
│  [User] ──อัพโหลดรูป──▶ [API /upload] ──store──▶ [Vercel Blob]         │
│                                                                          │
│  [User] ──ดู Figma──▶ [API /figma] ──fetch──▶ [Figma API]              │
│                              │                                           │
│                        ◀── files, pages, thumbnails                      │
│                                                                          │
│  [Anyone] ──เปิดลิงก์──▶ [/portfolio/[user]/[slug]] ──SSR──▶ [Neon DB] │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```
